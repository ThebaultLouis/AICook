import 'package:flutter/material.dart';

class RegimeDetailsPage extends StatelessWidget {
  const RegimeDetailsPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('coucou'),
      ),
    );
  }
}
